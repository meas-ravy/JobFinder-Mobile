import 'package:flutter/material.dart';

class RecruiterStatsPage extends StatelessWidget {
  const RecruiterStatsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
