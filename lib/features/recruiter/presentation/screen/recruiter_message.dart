import 'package:flutter/material.dart';

class RecruiterMessagePage extends StatelessWidget {
  const RecruiterMessagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
