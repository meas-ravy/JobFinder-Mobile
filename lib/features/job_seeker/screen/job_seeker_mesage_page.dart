import 'package:flutter/material.dart';

class JobSeekerMessagePage extends StatelessWidget {
  const JobSeekerMessagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Job Seeker Message"))),
    );
  }
}
