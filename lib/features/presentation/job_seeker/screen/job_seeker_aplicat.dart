import 'package:flutter/material.dart';

class JobSeekerAplicatPage extends StatelessWidget {
  const JobSeekerAplicatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Job Seeker Application"))),
    );
  }
}
